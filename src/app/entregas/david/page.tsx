'use client'

import { DeliveriesPage } from '@/components/deliveries/DeliveriesPage'

export default function DavidDeliveriesPage() {
  return (
    <DeliveriesPage
      responsible="david"
      title="Entregas a David"
      subtitle="Materiales entregados a David (subcontratista)"
    />
  )
}
