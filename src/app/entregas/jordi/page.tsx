'use client'

import { DeliveriesPage } from '@/components/deliveries/DeliveriesPage'

export default function JordiDeliveriesPage() {
  return (
    <DeliveriesPage
      responsible="jordi"
      title="Entregas a Jordi"
      subtitle="Materiales entregados a Jordi (subcontratista)"
    />
  )
}
